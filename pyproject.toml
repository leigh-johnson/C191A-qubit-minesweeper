[project]
name = "c191a-project"
version = "0.1.0"
description = ""
authors = [{ name = "Leigh Zaman leighzaman@berkeley.edu" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.11,<4"
dependencies = [
    "stim (>=1.15.0,<2.0.0)",
    "pulser (>=1.6.2,<2.0.0)",
    "pymatching (>=2.3.1,<3.0.0)",
    "sinter (>=1.15.0,<2.0.0)",
    "numpy (==2.2.6)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = ["ipykernel (>=7.0.1,<8.0.0)"]
